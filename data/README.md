# Local dataset

Place your downloaded **Kaggle cryptocurrency CSV** (and any related extracts) here.

- Files in this directory are **not** committed to git (see root `.gitignore`).
- Set `LOCAL_FILE_PATH` in the repo root `.env` to a file under this folder, e.g. `data/your_export.csv` (paths are resolved relative to the repo root).
